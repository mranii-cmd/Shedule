# Database (API connects to MySQL using these)
DB_HOST=mysql
DB_PORT=3306
DB_USER=edt_user
DB_PASS=verysecret
DB_NAME=edt_db

# MySQL official image init vars (used when the mysql container is created)
MYSQL_ROOT_PASSWORD=S@mya2598
MYSQL_DATABASE=edt_db

# API
PORT=4000
CORS_ORIGIN=http://localhost:8080
DB_CONN_LIMIT=10
MYSQL_USER=edt_user
MYSQL_PASSWORD=verysecret

# Admin / auth
ADMIN_USER=admin
ADMIN_PASSWORD=verysecret
# Option 1 (simple): plain password (dev-only)
#ADMIN_PASSWORD_HASH=658d0f63e501824d6c23e06d4bb95c71e7d704537c9d9272f488ac03a370d448

# Option 2 (recommended): use bcrypt hash instead of plain password
# ADMIN_PASSWORD_HASH=$2a$10$... (bcrypt hash)
# To produce a hash: node -e "console.log(require('bcryptjs').hashSync('verysecret', 10))"

# JWT secret and expiry (override defaults)
JWT_SECRET=change_this_secret_in_prod
JWT_EXPIRES_IN=8h